import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "Promptify — AI Voice Prompt Generator for Desktop",
  description: "Speak your idea. Get an expert-level AI prompt injected directly into any app. A floating desktop widget for Windows, inspired by Wispr Flow.",
  keywords: ["AI prompts", "voice dictation", "desktop widget", "Windows app", "AI assistant", "prompt engineering"],
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en" className={`${inter.variable} h-full`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
